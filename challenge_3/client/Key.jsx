import React, { Component } from "react";
import ReactDOM from "react-dom";

const Key = ({ number }) => {
  return <div className="border rounded m-1 text-center p-4">{number}</div>;
};

export default Key;
